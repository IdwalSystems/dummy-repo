@model IEnumerable<MSNK.Models.Modules.JProfilKategori>

@{
    ViewData["Title"] = "Profil Kategori";
}

<partial name="_JSIndex" />

<div class="card box-shadow">
    <div class="card card-body border">
        <partial name="_TitleIndexPartial" />

        <partial name="_SearchJadualIndexPartial" />

        <div class="table-responsive">
            <table class="table table-bordered table-hover text-nowrap table-sm nowrap compact" width="100%" id="IndexTable">
                <thead>
                    <tr>
                        <th width="5%">Bil</th>
                        <th class="perihal">
                            Perihal
                        </th>
                        <th width="5%">
                            Kod
                        </th>
                        <th width="10%">
                            Kadar Geran RM
                        </th>
                        <th width="5%"></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        var tabletype = item.FlHapus == 1 ? "table-danger" : null;
                        <tr class="@tabletype text-uppercase">
                            <td></td>
                            <td class="text-uppercase">
                                @item.Perihal
                            </td>
                            <td class="text-uppercase">
                                @item.Kod
                            </td>
                            <td class="text-right">
                                @Convert.ToDecimal(item.KadarGeran).ToString("#,##0.00")
                            </td>
                            <td width="5%" class="text-center action-button">
                                <div class="btn-group" role="group" aria-label="action">
                                    @if (item.FlHapus == 0)
                                    {
                                        <a asp-action="Details" asp-route-id="@item.Id"
                                           class="btn text-primary btn-sm bg-white" type="button"
                                           data-toggle="tooltip" data-placement="top"
                                           title="Ubah/Papar"><i class="fas fa-edit"></i></a>
                                        <a asp-action="Delete" asp-route-id="@item.Id"
                                           class="btn text-danger btn-sm bg-white" type="button"
                                           data-toggle="tooltip" data-placement="top"
                                           title="Hapus"><i class="fas fa-trash"></i></a>
                                    }
                                    else
                                    {
                                        if (User.IsInRole("Supervisor") || User.IsInRole("SuperAdmin"))
                                        {
                                            <a asp-action="Rollback" asp-route-id="@item.Id" class="btn text-info btn-sm bg-white" type="button" data-toggle="tooltip" data-placement="top" title="Rollback">
                                                <i class="fas fa-trash-restore"></i>
                                            </a>

                                        }
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
