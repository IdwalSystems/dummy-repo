@{
    ViewData["Title"] = "Halaman Utama";
}

@*div Header*@
<div class="row mb-0">
    <div class="col-12">
        <h3 class="text-left">Dashboard</h3>
    </div>
</div>
<hr class="mt-0" />
@*div Header end*@
@*<div class="text-center">
        <h1 class="display-4">Selamat Datang</h1>
        <p>
            @ViewBag.json
        </p>
        @if (User.Identity.IsAuthenticated)
        {

            @if (User.IsInRole("Admin"))
            {
                <h2>Peranan anda - Admin</h2>

            }
            else if (User.IsInRole("Supervisor"))
            {
                <h2>Peranan anda - Supervisor</h2>
            }
            else if (User.IsInRole("User"))
            {
                <h2>Peranan anda - User</h2>
            }
            else
            {
                <h2>Peranan anda - SuperAdmin</h2>
            }
        }
    </div>*@
    <div class="card box-shadow">
        <div class="card-body">
            <h5 class="text-purple">Status Terkini</h5>
            <!-- Nav tabs -->
            <ul class="nav nav-tabs">
                <li class="nav-item">
                    <a class="nav-link active" data-toggle="tab" href="#statusNotaMinta" id="tabNM">Nota Minta</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" data-toggle="tab" href="#StatusPO" id="tabPO">Pesanan Tempatan</a>
                </li>
            </ul>
            @*Tab panes*@
            <div class="tab-content">
                <div id="statusNotaMinta" class="tab-pane active">
                    <br>
                    <div class="form-row mb-3">
                        <div class="col-md-12">
                            <partial name="_StatusNotaMintaWidget" />
                        </div>
                    </div>
                </div>
                <div id="StatusPO" class="tab-pane fade">
                    <br>
                    <div class="form-row mb-3">
                        <div class="col-md-12">
                            <partial name="_StatusPOWidget" />
                        </div>
                    </div>
                </div>
                <div id="menu2" class="container tab-pane fade" hidden>
                    <br>
                    <h3>Menu 2</h3>
                    <p>Sed ut perspiciatis unde omnis iste natus error sit voluptatem accusantium doloremque laudantium, totam rem aperiam.</p>
                </div>
            </div>
        </div>
    </div>
    
@*<div class="form-row mb-3">
    <div class="col-md-12">
        <partial name="_StatusNotaMintaWidget" />
    </div>
</div>
<div class="form-row mb-3">
    <div class="col-md-12">
        <partial name="_StatusPOWidget" />
    </div>
</div>*@

