<script type="text/javascript">
    // onchange no Rujukan
    $("body").on('change', "#TarJana", function () {
        var tarJana = $("#TarJana").val();
        var noPBI = moment(tarJana).format('YYYYMM');
        if (tarJana != null || tarJana != undefined) {
            JsonKod(tarJana, noPBI);
        }

    });

    function JsonKod(tarJana, noPBI) {
        $.ajax({
            type: "POST",
            //url: "/AkPV/JsonGetKod",
            url: '@Url.Action("JsonGetKod", "AkCimbEFT")',
            data: { tarJana: tarJana, noPBI: noPBI },
            dataType: "json",
            success: function (msg) {
                if (msg.record) {
                    $("#NoPBI").val(msg.record);
                    $("#NamaFail").val(msg.record + ".txt");
                }
            },
            error: function (req, status, error) {
                alert(error);
            }
        });
    }
    // onchange no Rujukan end

    // jana kelompok
    function JanaKelompok() {
        $(".loading").prop('hidden', false);
        var loading = 0;
        var tarDari = $("#TarDari").val();
        var tarHingga = $("#TarHingga").val();

        var jumlah = $("#jumlahBaucer");
        var bilBaucer = $("#bilBaucer");

        if (tarHingga < tarDari)
        {
            $("#TarHingga").val(tarDari);
        }

        $.ajax({
            type: "POST",
            //url: "/AkPV/JsonGetKod",
            url: '@Url.Action("JsonGetBaucer", "AkCimbEFT")',
            data: { tarDari: tarDari, tarHingga: tarHingga },
            dataType: "json",
            success: function (msg) {
                if (msg.result == 'OK') {
                    //refresh updated table Baucer\
                    $("table tbody#tbodyBaucer").empty();
                    var sum = 0;
                    var bil = 0;
                    if (msg.table.length > 0) {
                        for (var i = 0; i < msg.table.length; i++) {
                            var item1 = msg.table[i];
                            var str = "<tr><td id='Indek' hidden>" + item1.indek +
                                "</td><td class='text-uppercase'>" + item1.indek +
                                "</td><td class='text-uppercase'>" + item1.noPV +
                                "</td><td class='text-uppercase'>" + item1.noKP +
                                "</td><td class='text-uppercase'>" + item1.noAkaun +
                                "</td><td class='text-uppercase'>" + item1.penerima +
                                "</td><td class='text-right'>" + (item1.amaun).toFixed(2) +
                                "</td><td>" +
                                "<button class='btn ac-danger btn-sm' type='button' id='btnRemove1' onclick='Remove1(this)'><i class='fas fa-trash' ></i></button>" +
                                "</td><tr>";
                            $("table tbody#tbodyBaucer").append(str);
                            sum += item1.amaun;
                            bil++;

                        }
                    }
                    jumlah.val(sum.toFixed(2));
                    bilBaucer.val(bil);

                    $(".loading").prop('hidden', true);
                }
                else {
                    toastr.error("kod EFT bagi bank " + msg.bank + " tidak diisi. Sila isi di Jadual Bank", "Ralat");
                    toastr.options.timeOut = 5000; // 5s
                    $(".loading").prop('hidden', true);
                }

            },
            error: function (req, status, error) {
                alert(error);
                $(".loading").prop('hidden', true);
            }
        });
    }
    // jana kelompok end

    // remove tbl baucer
    function Remove1(button) {
        var row = $(button).closest("TR");
        var indek = $("TD", row).eq(0).html();
        var noPV = $("TD", row).eq(2).html();
        var amaun = $("TD", row).eq(6).html();
        var jumlah = $("#jumlahBaucer");
        var bil = $("#bilBaucer");
        if (confirm("Hapus No Baucer : " + noPV + " ?")) {
            //Get the reference of the Table.
            var table = $("#tblBaucer")[0];

            var AkCimbEFT1 = {
                Indek: indek,
            }
            if (name != null) {
                $.ajax({
                    type: "POST",
                    url: '@Url.Action("RemoveAkCimbEFT1", "AkCimbEFT")',
                    data: AkCimbEFT1,
                    dataType: "json",
                    success: function (r) {
                        //Delete the Table row using it's Index.
                        table.deleteRow(row[0].rowIndex);
                        //Sum value to Total text field
                        var sum = 0;
                        sum = parseFloat(jumlah.val()) - parseFloat(amaun);
                        jumlah.val(sum.toFixed(2));

                        var bilBaucer = 0;
                        bilBaucer = parseFloat(bil.val()) - 1;
                        bil.val(bilBaucer);

                        toastr.success("No Baucer " + noPV+ " berjaya dihapuskan.")
                    }
                });
            }
        }
    }
    // remove tbl baucer end
</script>