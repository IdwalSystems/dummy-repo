<script type="text/javascript">
    //empty cart
    function EmptyCart() {
        $.ajax({
            type: "POST",
            //url: "/AkPV/JsonEmptyCart",
            url: '@Url.Action("JsonEmptyCart", "AkTerima")',
            dataType: "json",
            success: function (msg) {
                $("table tbody#tbodyObjek").empty();
                $("table tbody#tbodyInbois").empty();
                $("#jumlahObjek").val("0.00");
                $("#jumlahInbois").val("0.00");
                $("#txtKodAkaun option").prop('disabled', false);
                $("#txtKodAkaun option:selected").prop('disabled', true);
            },
            error: function (req, status, error) {
                alert(error);
            }
        });
    };
    //empty cart end

    // onchange no Rujukan
    $("body").on('change', "#ddlKW , #txtTahun", function () {
        var kodkw = $("#ddlKW").val();
        var year = $("#txtTahun").val();
        if (kodkw != 0) {
            JsonKod(kodkw, year);
        }

    });

    function JsonKod(id, year) {
        $.ajax({
            type: "POST",
            //url: "/AkTerima/JsonGetKod", // asal
            url: '@Url.Action("JsonGetKod", "AkTerima")', //baru
            data: { data: id, year: year },
            dataType: "json",
            success: function (msg) {
                $("#txtNoRujukan").val(msg.record);
            },
            error: function (req, status, error) {
                alert(error);
            }
        });
    }
    // onchange no Rujukan end

    // on change kod penghutang
    $("body").on('change', "#ddlKodPenghutang", function () {
        var kodPenghutang = $("#ddlKodPenghutang").val();
        var card = $("#cardInbois");
        if (kodPenghutang != 0) {
            card.removeAttr('hidden');
            $("#txtAkInvois").empty();
            JsonKodPenghutang(kodPenghutang);
        } else {
            //enable field
            $("#NoKp").prop('disabled', false);
            $("#Nama").prop('disabled', false);
            $("#Nama").prop('required', true);
            $("#Alamat1").prop('disabled', false);
            $("#Alamat2").prop('disabled', false);
            $("#Alamat3").prop('disabled', false);
            $("#Telefon").prop('disabled', false);
            $("#Emel").prop('disabled', false);
            $("#Poskod").prop('disabled', false);
            $("#Bandar").prop('disabled', false);

            //empty field
            $("#NoKp").val('');
            $("#Nama").val('');
            $("#Alamat1").val('');
            $("#Alamat2").val('');
            $("#Alamat3").val('');
            $("#Tel").val('');
            $("#Emel").val('');
            $("#Poskod").val('');
            $("#Bandar").val('');

            $("#btnSave").show();
            card.prop('hidden', true);
            EmptyCart();
        }

    });

    function JsonKodPenghutang(id) {
        if (id != null) {
            $.ajax({
                type: "POST",
                //url: "/AkPV/JsonGetPembekal",
                url: '@Url.Action("JsonGetPenghutang", "AkTerima")',
                data: { data: id },
                dataType: "json",
                success: function (msg) {
                    $.ajax({
                        type: "POST",
                        url: '@Url.Action("JsonGetInboisDikeluarkan", "AkTerima")',
                        data: { data: id },
                        datatype: "json",
                        success: function (r) {
                            var jkw = 0;
                            var jbahagian = 0;
                            var jNegeri = 0;
                            if (r.result == "OK") {
                                var select = new Option("-- Pilih Inbois --", 0, true, true);
                                $("#txtAkInvois").append(select);
                                if (r.record.length > 0) {
                                    for (var i = 0; i < r.record.length; i++) {
                                        var item1 = r.record[i];
                                        var option = new Option(item1.noInbois.substring(3), item1.id, true, true);
                                        $("#txtAkInvois").append(option);
                                        jkw = item1.jkwId;
                                        jbahagian = item1.jBahagianId;
                                        jNegeri = item1.akPenghutang.jNegeriId;

                                    }
                                }
                                $("#ddlKW").val(jkw).change();
                                $("#ddlBahagian").val(jbahagian).change();
                                $("#ddlNegeri").val(jNegeri).change();
                                $("#txtAkInvois").val(0).change();

                                //populate related fields

                                $("#NoKp").val(msg.record.kodSykt);
                                $("#Nama").val(msg.record.namaSykt);
                                $("#Alamat1").val(msg.record.alamat1);
                                $("#Alamat2").val(msg.record.alamat2);
                                $("#Alamat3").val(msg.record.alamat3);
                                $("#Tel").val(msg.record.telefon1);
                                $("#Emel").val(msg.record.emel);
                                $("#Poskod").val(msg.record.poskod);
                                $("#Bandar").val(msg.record.bandar);
                                // disable fields
                                $("#NoKp").prop('disabled', true);
                                $("#Nama").prop('disabled', true);
                                $("#Alamat1").prop('disabled', true);
                                $("#Alamat2").prop('disabled', true);
                                $("#Alamat3").prop('disabled', true);
                                $("#Tel").prop('disabled', true);
                                $("#Emel").prop('disabled', true);
                                $("#Poskod").prop('disabled', true);
                                $("#Bandar").prop('disabled', true);
                                $("#ddlKodPekerja").prop('disabled', true);
                                $("#ddlKodPekerja").val(0);
                                $("#Nama").prop('required', false);

                            }
                            else {
                                $("#ddlKodPenghutang").val(0).change();
                                toastr.error("Tiada inbois untuk penghutang ini", "Ralat");
                                toastr.options.timeOut = 5000; // 5s
                            }


                        },
                        error: function (error) {
                            alert(error);
                        }
                    });
                },
                error: function (req, status, error) {
                    alert(error);
                }
            });
        }
    }
    //on change Kod Penghutang end

    // on change no Inbois dikeluarkan
    $("body").on('change', "#txtAkInvois", function () {
        var noInbois = $("#txtAkInvois").val();

        if (noInbois != 0) {
            JsonNoInbois(noInbois);
        }

    });

    function JsonNoInbois(id) {
        if (id != null) {
            $.ajax({
                type: "POST",
                //url: "/AkPV/JsonGetAkBelian",
                url: '@Url.Action("JsonGetAkInvois", "AkTerima")',
                data: { data: id },
                dataType: "json",
                success: function (msg) {
                    $("#txtTarInbois").val(msg.record.tarikh);
                    $("#txtAmaunInbois").val(msg.record.jumlah.toFixed(2));
                },
                error: function (req, status, error) {
                    alert(error);
                }
            });
        }

    }

    //on change no Inbois pembekal end

    // onchange no KP
    $("body").on('change', "#NoKp", function () {
        var noKP = $("#NoKp").val();
        if (noKP != null) {
            JsonNoKP(noKP);
        }

    });

    function JsonNoKP(id) {
        $.ajax({
            type: "POST",
            //url: "/AkTerima/JsonGetKod", // asal
            url: '@Url.Action("JsonGetNoKP", "AkTerima")', //baru
            data: { data: id },
            dataType: "json",
            success: function (msg) {
                if (msg) {
                    $("#Nama").val(msg.record.nama);
                    $("#Alamat1").val(msg.record.alamat1);
                    $("#Alamat2").val(msg.record.alamat2);
                    $("#Alamat3").val(msg.record.alamat3);
                    $("#Poskod").val(msg.record.poskod);
                    $("#Bandar").val(msg.record.bandar);
                    $("#ddlNegeri").val(msg.record.jNegeriId).trigger('change');
                    $("#Tel").val(msg.record.tel);
                    $("#Emel").val(msg.record.emel);


                }

            },
            error: function (req, status, error) {
                alert(error);
            }
        });
    }
    // onchange no KP end

    // onchange CaraBayar
    $("body").on('change', "#ddlCaraBayar", function () {
        var caraBayar = $("#ddlCaraBayar").val();
        $("#txtAmaunUrusniaga").val("0.00");
        $("#txtNoCek").val("-");
        $("#txtJenisCek").val("");
        $("#txtKodBankCek").val("-");
        $("#txtTempatCek").val("-");
        $("#txtNoSlip").val("-");
        $("#txtTarSlip").val("");
        JsonCaraBayar(caraBayar);
    });

    function JsonCaraBayar(id) {
        if (id != null) {
            $.ajax({
                type: "POST",
                //url: "/AkTerima/JsonGetCaraBayar",
                url: '@Url.Action("JsonGetCaraBayar", "AkTerima")',
                data: { data: id },
                dataType: "json",
                success: function (msg) {
                    if (msg.record.kod == "C") {
                        // enable fields
                        $("#txtJenisCek").prop('disabled', false);
                        $("#txtKodBankCek").prop('disabled', false);
                        $("#txtTempatCek").prop('disabled', false);
                        $("#txtNoCek").prop('disabled', false);
                    }
                    else {
                        // disable fields
                        $("#txtJenisCek").prop('disabled', true);
                        $("#txtKodBankCek").prop('disabled', true);
                        $("#txtTempatCek").prop('disabled', true);
                        $("#txtNoCek").prop('disabled', true);
                    }

                },
                error: function (req, status, error) {
                    alert(error);
                }
            });
        }
    }
    // onchange CaraBayar end

    // on change no permohonan
    $("body").on('change', "#ddlPendahuluanPelbagai", function () {
        var kod = $("#ddlPendahuluanPelbagai").val();
        if (kod != 0) {
            JsonKodPendahuluan(kod);
        } else {
            //enable field
            $("#NoKp").prop('readonly', false);
            $("#Nama").prop('readonly', false);
            $("#Nama").prop('required', true);
            $("#Alamat1").prop('readonly', false);
            $("#Alamat2").prop('readonly', false);
            $("#Alamat3").prop('readonly', false);
            $("#Tel").prop('readonly', false);
            $("#Emel").prop('readonly', false);
            $("#Poskod").prop('readonly', false);
            $("#Bandar").prop('readonly', false);

            //empty field
            $("#NoKp").val('');
            $("#Nama").val('');
            $("#Alamat1").val('');
            $("#Alamat2").val('');
            $("#Alamat3").val('');
            $("#Tel").val('');
            $("#Emel").val('');
            $("#Poskod").val('');
            $("#Bandar").val('');

            $("#btnSave").show();
            EmptyCart();
        }

    });

    function JsonKodPendahuluan(id) {
        if (id != null) {
            $.ajax({
                type: "POST",
                //url: "/AkPV/JsonGetPembekal",
                url: '@Url.Action("JsonGetPendahuluan", "AkTerima")',
                data: { data: id, AkTerimaId: 0 },
                dataType: "json",
                success: function (msg) {
                    if (msg.result == "OK") {
                        $("#txtTahun").val(msg.record.tarMasuk.substring(0,4));
                        $("#ddlKW").val(msg.record.jkwId).change();
                        $("#ddlBahagian").val(msg.record.jBahagianId).change();
                        $("#ddlNegeri").val(msg.record.jNegeriId).change();

                        if (msg.record.suPekerja.jCaraBayarId != null) {
                            $("#ddlJCaraBayar").val(msg.record.suPekerja.jCaraBayarId).change();
                        }


                        //populate related fields
                        if (msg.record.suPekerja.noKp != null) {
                            $("#NoKp").val(msg.record.suPekerja.noKp);
                        }

                        $("#Nama").val(msg.record.suPekerja.nama);
                        $("#Alamat1").val(msg.record.suPekerja.alamat1);
                        $("#Alamat2").val(msg.record.suPekerja.alamat2);
                        $("#Alamat3").val(msg.record.suPekerja.alamat3);
                        $("#Tel").val(msg.record.suPekerja.telefonBimbit);
                        $("#Emel").val(msg.record.suPekerja.emel);
                        $("#Poskod").val(msg.record.suPekerja.poskod);
                        $("#Bandar").val(msg.record.suPekerja.bandar);
                        // disable fields
                        $("#NoKp").prop('readonly', true);
                        $("#Nama").prop('readonly', true);
                        $("#Alamat1").prop('readonly', true);
                        $("#Alamat2").prop('readonly', true);
                        $("#Alamat3").prop('readonly', true);
                        $("#Tel").prop('readonly', true);
                        $("#Emel").prop('readonly', true);
                        $("#Poskod").prop('readonly', true);
                        $("#Bandar").prop('readonly', true);
                        $("#Nama").prop('required', false);

                        $("table tbody#tbodyObjek").empty();
                        var jumlah = $("#jumlahObjek");
                        var sum = 0;
                        var str = "<tr><td id='akCartaId' hidden>" + msg.record.akCartaId +
                            "</td><td>" + msg.record.akCarta.kod + " - " + msg.record.akCarta.perihal +
                            "</td><td class='text-right'>" + (msg.record.jumLulus).toFixed(2) +
                                "</td><td>" +
                                "<button class='btn ac-primary btn-sm' type='button' id='btnUpdate1' data-toggle='modal' data-target='#modalObjek' onclick='Update1(this)'><i class='fa fa-edit' ></i></button>" +
                                "</td><td>" +
                                "<button class='btn ac-danger btn-sm' type='button' id='btnRemove1' onclick='Remove1(this)'><i class='fas fa-trash' ></i></button>" +
                                "</td><tr>";
                            $("table tbody#tbodyObjek").append(str);
                        sum += msg.record.jumLulus;

                        $("#txtKodAkaun option[value=" + msg.record.akCartaId + "]").prop('disabled', true);

                        jumlah.val(sum.toFixed(2));

                    }
                    //else {
                    //    $("#ddlPendahuluanPelbagai").val(0).change();
                    //    toastr.error("Peruntukan untuk kod akaun ini tidak mencukupi", "Ralat");
                    //    toastr.options.timeOut = 2000; // 2s
                    //}
                },
                error: function (req, status, error) {
                    alert(error);
                }
            });
        }
    }
    //on change no permohonan end

    //add tbl objek
    $("body").on("click", "#btnAdd1", function () {
        //Reference the Name and Country TextBoxes.
        var txtKodAkaun = $("#txtKodAkaun");
        if (txtKodAkaun.val() != null) {
            GetCarta(txtKodAkaun.val());
        }

    });

    function GetCarta(id) {
        var Objek = {
            Id: id
        }
        if (id != "") {
            //Send the JSON array to Controller using AJAX.
            $.ajax({
                type: "POST",
                //url: "/AkTerima/GetCarta",
                url: '@Url.Action("GetCarta", "AkTerima")',
                data: Objek,
                dataType: "json",
                success: function (r) {
                    //Reference the TextBoxes.
                    var txtKodAkaun = $("#txtKodAkaun");

                    var txtAmaunObjek = $("#txtAmaunObjek");
                    var txtJumlah = $("#jumlahObjek");

                    //Sum value to Total text field
                    var sum = 0;
                    sum = parseFloat(txtAmaunObjek.val()) + parseFloat(txtJumlah.val());
                    txtJumlah.val(sum.toFixed(2));

                    //Get the reference of the Table's TBODY element.
                    var tBody = $("#tblObjek > TBODY")[0];
                    //Add Row.
                    var row = tBody.insertRow(-1);

                    //Add akCartaId cell.
                    var cell = $(row.insertCell(-1));
                    cell.html(r.record.id);
                    cell.prop("hidden", !this.checked);

                    //Add kod cell.
                    var cell = $(row.insertCell(-1));
                    cell.html(r.record.kod + ' - ' + r.record.perihal);

                    //Add amaun cell.
                    var cell = $(row.insertCell(-1));
                    cell.html(parseFloat(txtAmaunObjek.val()).toFixed(2));

                    //Add Edit Button cell.
                    cell = $(row.insertCell(-1));

                    var btnUpdate1 = $("<button class='btn ac-primary btn-sm' type='button' id='btnUpdate1' onclick='Update1(this)'><i class='fas fa-edit'></i></button>");
                    cell.append(btnUpdate1);

                    //Add Button cell.
                    cell = $(row.insertCell(-1));
                    var btnRemove1 = $("<button class='btn ac-danger btn-sm' type='button' id='btnRemove1' onclick='Remove1(this)'><i class='fas fa-trash'></i></button>");
                    cell.append(btnRemove1);

                    var AkTerima1 = {
                        AkCartaId: txtKodAkaun.val(),
                        Amaun: txtAmaunObjek.val()
                    }

                    $.ajax({
                        type: "POST",
                        //url: "/AkTerima/SaveAkTerima1",
                        url: '@Url.Action("SaveAkTerima1", "AkTerima")',
                        data: AkTerima1,
                        dataType: "json",
                        success: function (r) {
                            //Clear the TextBoxes.
                            txtKodAkaun.val("").trigger('change');
                            txtAmaunObjek.val("0.00");
                            checkJumlah();
                        }
                    });
                    $("#txtKodAkaun option[value=" + txtKodAkaun.val() + "]").prop('disabled', true);

                }
            });
        }

    }
    //add tbl objek end
    //add tbl Urusniaga
    $("body").on("click", "#btnAdd2", function () {
        //Reference the Name and Country TextBoxes.
        var ddlCaraBayar = $("#ddlCaraBayar");
        if (ddlCaraBayar.val() != null) {
            GetCaraBayar(ddlCaraBayar.val());
        }

    });

    function GetCaraBayar(id) {
        var JCaraBayar = {
            Id: id
        }
        if (id != "") {
            //Send the JSON array to Controller using AJAX.
            $.ajax({
                type: "POST",
                //url: "/AkTerima/GetCaraBayar",
                url: '@Url.Action("GetCaraBayar", "AkTerima")',
                data: JCaraBayar,
                dataType: "json",
                success: function (r) {
                    //Reference the TextBoxes.
                    var ddlCaraBayar = $("#ddlCaraBayar");
                    var txtAmaunUrusniaga = $("#txtAmaunUrusniaga");
                    var txtNoCek = $("#txtNoCek");
                    var txtJenisCek = $("#txtJenisCek");
                    var txtKodBankCek = $("#txtKodBankCek");
                    var txtTempatCek = $("#txtTempatCek");
                    var txtNoSlip = $("#txtNoSlip");
                    var txtTarSlip = $("#txtTarSlip");

                    var txtJumlah = $("#jumlahUrusniaga");

                    //Sum value to Total text field
                    var sum = 0;
                    sum = parseFloat(txtAmaunUrusniaga.val()) + parseFloat(txtJumlah.val());
                    txtJumlah.val(sum.toFixed(2));

                    //Get the reference of the Table's TBODY element.
                    var tBody = $("#tblUrusniaga > TBODY")[0];
                    //Add Row.
                    var row = tBody.insertRow(-1);

                    //Add caraBayarId cell.
                    var cell = $(row.insertCell(-1));
                    cell.html(r.record.id);
                    cell.prop("hidden", !this.checked);

                    //Add caraBayar cell.
                    var cell = $(row.insertCell(-1));
                    cell.html(r.record.perihal);

                    //Add Amaun cell.
                    var cell = $(row.insertCell(-1));
                    cell.html(parseFloat(txtAmaunUrusniaga.val()).toFixed(2));

                    //Add NoCek cell.
                    var cell = $(row.insertCell(-1));
                    cell.html(txtNoCek.val());

                    //Add JenisCek cell.
                    var cell = $(row.insertCell(-1));
                    if ($("#txtJenisCek").val() == null) {
                        cell.html("");
                    } else {
                        cell.html($("#txtJenisCek option:selected").text().toUpperCase());
                    }


                    //Add KodBankCek cell.
                    var cell = $(row.insertCell(-1));
                    cell.html(txtKodBankCek.val());

                    //Add TempatCek cell.
                    var cell = $(row.insertCell(-1));
                    cell.html(txtTempatCek.val());

                    //Add NoSlip cell.
                    var cell = $(row.insertCell(-1));
                    cell.html(txtNoSlip.val());

                    //Add TarSlip cell.
                    var cell = $(row.insertCell(-1));
                    if (txtTarSlip.val() == null || txtTarSlip.val() == '') {
                        cell.html('');
                    } else {

                        cell.html(showDate(txtTarSlip.val()));
                    }

                    //Add Edit Button cell.
                    cell = $(row.insertCell(-1));

                    var btnUpdate2 = $("<button class='btn ac-primary btn-sm' type='button' id='btnUpdate2' onclick='Update2(this)'><i class='fas fa-edit'></i></button>");
                    cell.append(btnUpdate2);

                    //Add Button cell.
                    cell = $(row.insertCell(-1));
                    var btnRemove2 = $("<button class='btn ac-danger btn-sm' type='button' id='btnRemove1' onclick='Remove2(this)'><i class='fas fa-trash'></i></button>");
                    cell.append(btnRemove2);

                    var AkTerima2 = {
                        JCaraBayarId: ddlCaraBayar.val(),
                        Amaun: txtAmaunUrusniaga.val(),
                        NoCek: txtNoCek.val(),
                        JenisCek: txtJenisCek.val(),
                        KodBankCek: txtKodBankCek.val(),
                        TempatCek: txtTempatCek.val(),
                        NoSlip: txtNoSlip.val(),
                        TarSlip: txtTarSlip.val()
                    }
                    $.ajax({
                        type: "POST",
                        //url: "/AkTerima/SaveAkTerima2",
                        url: '@Url.Action("SaveAkTerima2", "AkTerima")',
                        data: AkTerima2,
                        dataType: "json",
                        success: function (r) {
                            //Clear the TextBoxes.
                            ddlCaraBayar.val("").trigger('change');
                            txtAmaunUrusniaga.val("0.00");
                            txtNoCek.val("-");
                            txtJenisCek.val("");
                            txtKodBankCek.val("-");
                            txtTempatCek.val("-");
                            txtNoSlip.val("-");
                            txtTarSlip.val("");
                            checkJumlah();
                        }
                    });
                    $("#ddlCaraBayar option[value=" + ddlCaraBayar.val() + "]").prop('disabled', true);

                }
            });
        }

    }
    //add tbl urusniaga end

    //remove tbl objek
    function Remove1(button) {
        //Determine the reference of the Row using the Button.
        var row = $(button).closest("TR");
        var name = $("TD", row).eq(0).html();
        var amaun = $("TD", row).eq(2).html();
        var txtJumlah = $("#jumlahObjek");
        if (confirm("Hapus kod Akaun : " + name + " ?")) {
            //Get the reference of the Table.
            var table = $("#tblObjek")[0];

            //Sum value to Total text field
            var sum = 0;
            sum = parseFloat(txtJumlah.val()) - parseFloat(amaun);
            txtJumlah.val(sum.toFixed(2));

            var AkTerima1 = {
                AkCartaId: name,
            }
            if (name != null) {
                $.ajax({
                    type: "POST",
                    //url: "/AkTerima/RemoveAkTerima1",
                    url: '@Url.Action("RemoveAkTerima1", "AkTerima")',
                    data: AkTerima1,
                    dataType: "json",
                    success: function (r) {
                        //Delete the Table row using it's Index.
                        table.deleteRow(row[0].rowIndex);
                        checkJumlah();
                    }
                });
            }
            $("#txtKodAkaun option[value=" + name + "]").prop('disabled', false);

        }
    };
    //remove tbl objek end

    //remove tbl urusniaga
    function Remove2(button) {
        //Determine the reference of the Row using the Button.
        var row = $(button).closest("TR");
        var name = $("TD", row).eq(0).html();
        var amaun = $("TD", row).eq(2).html();
        var txtJumlah = $("#jumlahUrusniaga");

        if (confirm("Hapus : " + name + " ?")) {
            //Get the reference of the Table.
            var table = $("#tblUrusniaga")[0];

            //Sum value to Total text field
            var sum = 0;
            sum = parseFloat(txtJumlah.val()) - parseFloat(amaun);
            txtJumlah.val(sum.toFixed(2));

            var AkTerima2 = {
                JCaraBayarId: name,
            }
            if (name != null) {
                $.ajax({
                    type: "POST",
                    //url: "/AkTerima/RemoveAkTerima2",
                    url: '@Url.Action("RemoveAkTerima2", "AkTerima")',
                    data: AkTerima2,
                    dataType: "json",
                    success: function (r) {
                        //Delete the Table row using it's Index.
                        table.deleteRow(row[0].rowIndex);
                        checkJumlah();
                    }
                });
            }
            $("#ddlCaraBayar option[value=" + name + "]").prop('disabled', false);
        }
    };
    //remove tbl urusniaga end

    //update tbl objek
    function Update1(button) {
        //Determine the reference of the Row using the Button.
        var row = $(button).closest("TR");
        var akCartaId = $("TD", row).eq(0).html();

        var AkTerima1 = {
            AkCartaId: akCartaId
        }
        if (name != null) {
            $.ajax({
                type: "POST",
                //url: "/AkTerima/GetAnItemCartAkTerima1",
                url: '@Url.Action("GetAnItemCartAkTerima1", "AkTerima")',
                data: AkTerima1,
                dataType: "json",
                success: function (r) {
                    //insert data in modal objek
                    $("#moId1").val(r.record.id);
                    $("#moKodAkaun").val(r.record.akCartaId);
                    $("#moAmaun1").val(r.record.amaun.toFixed(2));

                    //show modal objek
                    $("#modalObjek").modal({ backdrop: 'static', keyboard: false });

                }
            });
        }
    };

    function modalUpdate1() {
        var id = $("#moId1").val();
        var amaun = $("#moAmaun1").val();
        var akCartaId = $("#moKodAkaun").val();
        var jumlah = $("#jumlahObjek");

        var AkTerima1 = {
            Id: id,
            akCartaId: akCartaId,
            Amaun: amaun
        }
        $.ajax({
            type: "POST",
            //url: "/AkTerima/SaveCartAkTerima1",
            url: '@Url.Action("SaveCartAkTerima1", "AkTerima")',
            data: AkTerima1,
            dataType: "json",
            success: function (r) {
                //insert notification here

                $.ajax({
                    type: "POST",
                    //url: "/AkTerima/GetAllItemCartAkTerima1",
                    url: '@Url.Action("GetAllItemCartAkTerima1", "AkTerima")',
                    dataType: "json",
                    success: function (r) {

                        //refresh updated table objek
                        $("table tbody#tbodyObjek").empty();
                        var sum = 0;
                        if (r.record.length > 0) {
                            for (var i = 0; i < r.record.length; i++) {
                                var item1 = r.record[i];
                                var str = "<tr><td id='akCartaId' hidden>" + item1.akCartaId +
                                    "</td><td class='tex-uppercase'>" + item1.akCarta.kod + " - " + item1.akCarta.perihal +
                                    "</td><td class='text-right'>" + (item1.amaun).toFixed(2) +
                                    "</td><td>" +
                                    "<button class='btn ac-primary btn-sm' type='button' id='btnUpdate1' data-toggle='modal' data-target='#modalObjek' onclick='Update1(this)'><i class='fa fa-edit' ></i></button>" +
                                    "</td><td>" +
                                    "<button class='btn ac-danger btn-sm' type='button' id='btnRemove1' onclick='Remove1(this)'><i class='fas fa-trash' ></i></button>" +
                                    "</td><tr>";
                                $("table tbody#tbodyObjek").append(str);
                                sum += item1.amaun;
                            }
                        }

                        jumlah.val(sum.toFixed(2));
                        checkJumlah();
                    }
                });

            }
        });
    };
    //update tbl objek end

    //update tbl urusniaga
    function Update2(button) {
        //Determine the reference of the Row using the Button.
        var row = $(button).closest("TR");
        var jCaraBayarId = $("TD", row).eq(0).html();
        var jenisBayar = $("TD", row).eq(1).html();

        var AkTerima2 = {
            JCaraBayarId: jCaraBayarId
        }
        $.ajax({
            type: "POST",
            //url: "/AkTerima/GetAnItemCartAkTerima2",
            url: '@Url.Action("GetAnItemCartAkTerima2", "AkTerima")',
            data: AkTerima2,
            dataType: "json",
            success: function (r) {
                //insert data in modal urusniaga
                $("#moId2").val(jCaraBayarId);
                $("#moCaraBayar").val(r.record.jCaraBayarId);
                $("#moAmaun2").val(r.record.amaun.toFixed(2));
                $("#moNoCek").val(r.record.noCek);
                $("#moJenisCek").val(r.record.jenisCek);
                $("#moKodBankCek").val(r.record.kodBankCek);
                $("#moTempatCek").val(r.record.tempatCek);
                $("#moNoSlip").val(r.record.noSlip);
                $("#moTarSlip").val(r.record.tarSlip);

                // check if jenis bayar equal to cek, enable all related cek fields, otherwise disabled it
                if (jenisBayar.indexOf("CEK") >= 0) {
                    // enable fields
                    $("#moNoCek").prop('disabled', false);
                    $("#moJenisCek").prop('disabled', false);
                    $("#moKodBankCek").prop('disabled', false);
                    $("#moTempatCek").prop('disabled', false);
                }
                else {
                    // disable fields
                    $("#moNoCek").prop('disabled', true);
                    $("#moJenisCek").prop('disabled', true);
                    $("#moKodBankCek").prop('disabled', true);
                    $("#moTempatCek").prop('disabled', true);
                }
                // end check

                //show modal Urusniaga
                $("#modalUrusniaga").modal({ backdrop: 'static', keyboard: false });

            }
        });
    };

    function modalUpdate2() {
        var id = $("#moId2").val();
        var amaun = $("#moAmaun2").val();
        var noCek = $("#moNoCek").val();
        var jenisCek = $("#moJenisCek").val();
        var kodBankCek = $("#moKodBankCek").val();
        var tempatCek = $("#moTempatCek").val();
        var noSlip = $("#moNoSlip").val();
        var tarSlip = $("#moTarSlip").val();
        var akTerimaId = $("#Id").val();
        var jumlah = $("#jumlahUrusniaga");

        var AkTerima2 = {
            JCaraBayarId: id,
            AkTerimaId: akTerimaId,
            Amaun: amaun,
            NoCek: noCek,
            JenisCek: jenisCek,
            KodBankCek: kodBankCek,
            TempatCek: tempatCek,
            NoSlip: noSlip,
            TarSlip: tarSlip
        }
        $.ajax({
            type: "POST",
            //url: "/AkTerima/SaveCartAkTerima2",
            url: '@Url.Action("SaveCartAkTerima2", "AkTerima")',
            data: AkTerima2,
            dataType: "json",
            success: function (r) {
                //insert notification here

                $.ajax({
                    type: "POST",
                    //url: "/AkTerima/GetAllItemCartAkTerima2",
                    url: '@Url.Action("GetAllItemCartAkTerima2", "AkTerima")',
                    dataType: "json",
                    success: function (r) {

                        //refresh updated table Urusniaga
                        $("table tbody#tbodyUrusniaga").empty();
                        var sum = 0;
                        if (r.record.length > 0) {
                            for (var i = 0; i < r.record.length; i++) {
                                var item1 = r.record[i];
                                var tarSlip = "";
                                if (item1.tarSlip == null) {
                                } else {
                                    tarSlip = showDate(item1.tarSlip);
                                }
                                var str = "<tr><td id='jCaraBayarId' hidden>" + item1.jCaraBayarId +
                                    "</td><td class='text-uppercase'>" + item1.jCaraBayar.perihal +
                                    "</td><td class='text-right'>" + (item1.amaun).toFixed(2) +
                                    "</td><td>" + item1.noCek +
                                    "</td><td class='text-uppercase'>" + item1.jenisCek +
                                    "</td><td class='text-uppercase'>" + item1.kodBankCek +
                                    "</td><td class='text-uppercase'>" + item1.tempatCek +
                                    "</td><td>" + item1.noSlip +
                                    "</td><td>" + tarSlip +
                                    "</td><td>" +
                                    "<button class='btn ac-primary btn-sm' type='button' id='btnUpdate2' data-toggle='modal' data-target='#modalUrusniaga' onclick='Update2(this)'><i class='fa fa-edit' ></i></button>" +
                                    "</td><td>" +
                                    "<button class='btn ac-danger btn-sm' type='button' id='btnRemove2' onclick='Remove2(this)'><i class='fas fa-trash' ></i></button>" +
                                    "</td><tr>";
                                $("table tbody#tbodyUrusniaga").append(str);
                                sum += item1.amaun;
                            }
                        }
                        jumlah.val(sum.toFixed(2));
                        checkJumlah();
                    }
                });

            }
        });
    };
    //update tbl urusniaga end

    //validation form
    (function () {
        'use strict';
        window.addEventListener('load', function () {
            // Fetch all the forms we want to apply custom Bootstrap validation styles to
            var forms = document.getElementsByClassName('needs-validation');
            // Loop over them and prevent submission
            Array.prototype.filter.call(forms, function (form) {
                form.addEventListener('submit', function (event) {
                    if (form.checkValidity() === false) {
                        event.preventDefault();
                        event.stopPropagation();
                    }
                    form.classList.add('was-validated');
                }, false);
            });
        }, false);
    })();

    $(document).ready(function () {
        $.validator.setDefaults({
            errorElement: "span",
            errorClass: "help-block",
            //	validClass: 'stay',
            highlight: function (element, errorClass) {
                $(element).addClass(errorClass); //.removeClass(errorClass);
                $(element).closest('.form-group').removeClass('has-success').addClass('has-error');
            },
            unhighlight: function (element, errorClass) {
                $(element).removeClass(errorClass); //.addClass(validClass);
                $(element).closest('.form-group').removeClass('has-error').addClass('has-success');
            },

            errorPlacement: function (error, element) {
                if (element.parent('.input-group').length) {
                    error.insertAfter(element.parent());
                } else {
                    error.insertAfter(element);
                }

            },

        });

        // on change jenis resit
        $('input[type=radio][name=FlJenisTerima]').on('change', function () {
            
            $("#ddlPendahuluanPelbagai").val(0).change();
            $("#ddlKodPenghutang").val(0).change();
            $("#txtPerihal").val("");
            EmptyCart();
            switch ($(this).val()) {
                case "0":
                    $("#divPenghutang").prop('hidden', true);
                    break;
                case "1":
                    $("#divPenghutang").removeAttr('hidden');
                    break;
                case "2":
                    break;
                case "3":
                    break;
                case "4":
                    break;
                case "6":
                    break;
            }
        });
        //on change jenis resit end

    });
    //validation form end

    // check if jumlahObjek is equal to jumlahUrusniaga
    function checkJumlah() {
        var jumlahObjek = $("#jumlahObjek").val();
        var jumlahUrusniaga = $("#jumlahUrusniaga").val();

        if (jumlahObjek == 0.00 && jumlahUrusniaga == 0.00) {
            $("#btnSave").show();
        } else {
            if (jumlahObjek == jumlahUrusniaga) {
                $("#btnSave").show();
            }
            else {
                $("#btnSave").hide();
            }
        }
    }
    // check end

    //add tbl Inbois
    $("body").on("click", "#btnAdd2", function () {
        //Reference the Name and Country TextBoxes.
        var ddlAkInvois = $("#txtAkInvois");
        if (ddlAkInvois.val() != null) {
            GetInbois(ddlAkInvois.val());
        }

    });

    function GetInbois(id) {
        var AkInvois = {
            Id: id
        }
        if (id != "") {
            //Send the JSON array to Controller using AJAX.
            $.ajax({
                type: "POST",
                //url: "/AkPV/GetAkBelian",
                url: '@Url.Action("GetAkInvois", "AkTerima")',
                data: AkInvois,
                dataType: "json",
                success: function (r) {
                    //Reference the TextBoxes.
                    var ddlAkInvois = $("#txtAkInvois");
                    var txtTarInbois = $("#txtTarInbois");
                    var txtAmaunInbois = $("#txtAmaunInbois");
                    var txtJumlah = $("#jumlahInbois");

                    var AkTerima3 = {
                        AkInvoisId: ddlAkInvois.val(),
                        Amaun: txtAmaunInbois.val()
                    }
                    var tahun = $("#txtTahun").val();
                    var jKWId = $("#ddlKW").val();
                    var jBahagianId = $("#ddlBahagian").val();
                    var myData = {};
                    myData.AkTerima3 = AkTerima3;
                    myData.tahun = tahun;
                    myData.jKWId = jKWId;
                    myData.jBahagianId = jBahagianId;

                    $.ajax({
                        type: "POST",
                        url: '@Url.Action("SaveAkTerima3", "AkTerima")',
                        data: myData,
                        dataType: "json",
                        success: function (b) {
                            if (b.result == "OK") {
                                //Sum value to Total text field
                                var sum = 0;
                                sum = parseFloat(txtAmaunInbois.val()) + parseFloat(txtJumlah.val());
                                txtJumlah.val(sum.toFixed(2));

                                //Get the reference of the Table's TBODY element.
                                var tBody = $("#tblInbois > TBODY")[0];
                                //Add Row.
                                var row = tBody.insertRow(-1);

                                //Add AkBelianId cell.
                                var cell = $(row.insertCell(-1));
                                cell.html(r.record.id);
                                cell.prop("hidden", !this.checked);

                                //Add No inbois cell.
                                var cell = $(row.insertCell(-1));
                                cell.html(r.record.noInbois.substring(3));

                                //Add TarInbois cell.
                                var cell = $(row.insertCell(-1));
                                if (txtTarInbois.val() == null || txtTarInbois.val() == '') {
                                    cell.html('');
                                } else {
                                    cell.html(showDate(txtTarInbois.val()));
                                }

                                //Add amaun inbois cell.
                                var cell = $(row.insertCell(-1));
                                cell.html(parseFloat(txtAmaunInbois.val()).toFixed(2));
                                cell.addClass('text-right');

                                //Add Edit Button cell.
                                cell = $(row.insertCell(-1));

                                var btnUpdate2 = $("<button class='btn ac-primary btn-sm' type='button' id='btnUpdate3' onclick='Update3(this)'><i class='fas fa-edit'></i></button>");
                                cell.append(btnUpdate2);

                                //Add Button cell.
                                cell = $(row.insertCell(-1));
                                var btnRemove2 = $("<button class='btn ac-danger btn-sm' type='button' id='btnRemove3' onclick='Remove3(this)'><i class='fas fa-trash'></i></button>");
                                cell.append(btnRemove2);

                                //displayObjekList(r.record.akBelian1);

                                $("#txtAkInvois option[value=" + ddlAkInvois.val() + "]").prop('disabled', true);

                                //Clear the TextBoxes.
                                ddlAkInvois.val("0").trigger('change');
                                txtTarInbois.val("");
                                txtAmaunInbois.val("0.00");
                                checkJumlah();


                            }
                            else {
                                toastr.error("Bajet untuk kod akaun ini tidak mencukupi.", "Ralat");
                                toastr.options.timeOut = 5000; // 5s
                            }

                        }
                    });

                }
            });
        }

    }
    //add tbl Inbois end

    //remove tbl Inbois
    function Remove3(button) {
        //Determine the reference of the Row using the Button.
        var row = $(button).closest("TR");
        var name = $("TD", row).eq(0).html();
        var amaun = $("TD", row).eq(3).html();
        var txtJumlah = $("#jumlahInbois");

        if (confirm("Hapus : " + name + " ?")) {
            //Get the reference of the Table.
            var table = $("#tblInbois")[0];

            //Sum value to Total text field
            var sum = 0;
            sum = parseFloat(txtJumlah.val()) - parseFloat(amaun);
            txtJumlah.val(sum.toFixed(2));

            var AkTerima3 = {
                AkTerimaId: name
            }
            if (name != null) {
                $.ajax({
                    type: "POST",
                    url: '@Url.Action("RemoveAkTerima3", "AkTerima")',
                    data: AkTerima3,
                    dataType: "json",
                    success: function (r) {
                        //Delete the Table row using it's Index.
                        table.deleteRow(row[0].rowIndex);
                        checkJumlah();
                    }
                });
            }
            $("#txtAkInvois option[value=" + name + "]").prop('disabled', false);
        }
    };
    //remove tbl Inbois end

    //update tbl Inbois
    function Update3(button) {
        //Determine the reference of the Row using the Button.
        var row = $(button).closest("TR");
        var akInvoisId = $("TD", row).eq(0).html();
        var AkTerima3 = {
            AkTerimaId: akTerimaId
        }
        $.ajax({
            type: "POST",
            url: '@Url.Action("GetAnItemCartAkTerima3", "AkTerima")',
            data: AkTerima3,
            dataType: "json",
            success: function (r) {
                //insert data in modal urusniaga
                $("#moId3").val(r.record.akInvoisId);
                $("#moAkInvois").val(r.record.akInvoisId);
                $("#moAmaun3").val(r.record.amaun.toFixed(2));

                //show modal objek
                $("#modalInbois").modal({ backdrop: 'static', keyboard: false });

            }
        });
    };

    function modalUpdate3() {
        var id = $("#moId3").val();
        var amaun = $("#moAmaun3").val();
        var akTerimaId = $("#Id").val();
        var jumlah = $("#jumlahInbois");

        var AkTerima3 = {
            AkInvoisId: id,
            AkTerimaId: akTerimaId,
            Amaun: amaun
        }
        $.ajax({
            type: "POST",
            url: '@Url.Action("SaveCartAkTerima3", "AkTerima3")',
            data: AkTerima3,
            dataType: "json",
            success: function (r) {
                //insert notification here

                $.ajax({
                    type: "POST",
                    url: '@Url.Action("GetAllItemCartAkTerima3", "AkTerima")',
                    dataType: "json",
                    success: function (r) {

                        //refresh updated table Inbois
                        $("table tbody#tbodyInbois").empty();
                        var sum = 0;
                        if (r.record.length > 0) {
                            for (var i = 0; i < r.record.length; i++) {
                                var item1 = r.record[i];
                                var str = "<tr><td id='akInvoisId' hidden>" + item1.akInvoisId +
                                    "</td><td>" + item1.akInvois.noInbois.substring(3) +
                                    "</td><td>" + showDate(item1.akInvois.tarikh) +
                                    "</td><td class='text-right'>" + (item1.amaun).toFixed(2) +
                                    "</td><td>" +
                                    "<button class='btn ac-primary btn-sm' type='button' id='btnUpdate3' data-toggle='modal' data-target='#modalInbois' onclick='Update3(this)'><i class='fa fa-edit' ></i></button>" +
                                    "</td><td>" +
                                    "<button class='btn ac-danger btn-sm' type='button' id='btnRemove3' onclick='Remove3(this)'><i class='fas fa-trash' ></i></button>" +
                                    "</td><tr>";
                                $("table tbody#tbodyInbois").append(str);
                                sum += item1.amaun;
                            }
                        }
                        jumlah.val(sum.toFixed(2));
                        checkJumlah();
                    }
                });
            }
        });
    };
    //update tbl Inbois end
</script>