@model MSNK.Models.Modules.PrintModel.Reporting.LPV001PrintModel

@{
    ViewData["Title"] = "Cetakan LPV00101";
    Layout = "~/Views/Shared/_PrintLayout.cshtml";
    CompanyDetails company = new CompanyDetails();

    var bil = 0;
}
<style>
    .flexrow {
        display: -webkit-box;
        display: -webkit-flex;
        display: flex !important;
    }

        .flexrow > div {
            -webkit-box-flex: 1;
            -webkit-flex: 1;
            flex: 1;
        }

    .image-container {
        width: 80px;
        height: 80px;
        text-align: center;
    }

        .image-container img {
            height: 100%;
        }

    h1 {
        font-size: 1.5rem;
    }

    h2 {
        font-size: 1.3rem;
    }

    h3 {
        font-size: 1.2rem;
    }

    h4 {
        font-size: 1.1rem;
    }

    h5 {
        font-size: 1rem;
    }

    h6 {
        font-size: 0.8rem;
    }

    p {
        font-size: 0.6rem;
    }

    .topic tr {
        line-height: 0.5rem;
    }

    .center {
        padding: 70px 0;
        border: 3px solid green;
        text-align: center;
    }

    .notbold {
        font-weight: normal
    }

    table tbody tr td {
        font-size: 0.8rem;
        line-height: 1.0rem;
    }

    table thead tr th {
        font-size: 0.8rem;
        line-height: 1.0rem;
    }

    .breakhere {
        page-break-after: always
    }

    thead {
        display: table-header-group
    }

    tfoot {
        display: table-row-group
    }

    tr {
        page-break-inside: avoid
    }
</style>

<table class="table table-bordered table-sm topic">
    <thead>
        <tr class="bg-light">
            <th class="text-center font-weight-bold" colspan="11">Bil  </th>
        </tr>
        <tr class="bg-light">
            <th class="text-center font-weight-bold" colspan="11">Bil  </th>
        </tr>
        <tr class="bg-light">
            <th class="text-center font-weight-bold" colspan="11">Bil  </th>
        </tr>
        <tr class="bg-secondary">
            <th class="text-center font-weight-bold text-white">BIL  </th>
            <th class="text-center font-weight-bold text-white">TARIKH </th>
            <th class="text-center font-weight-bold text-white">NO BAUCER</th>
            <th class="text-center font-weight-bold text-white">PENERIMA </th>

            <th class="text-center font-weight-bold text-white">KOD AKAUN</th>
            <th class="text-center font-weight-bold text-white">PERIHALAN AKAUN</th>
            <th class="text-center font-weight-bold text-white">CARA BAYAR</th>
            <th class="text-center font-weight-bold text-white">NO CEK</th>
            <th class="text-center font-weight-bold text-white">TARIKH CEK</th>
            <th class="text-center font-weight-bold text-white">AMAUN RM</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.AkPV)
        {
            @foreach (var item1 in item.AkPV1)
            {

                // if data is Batal, change row color to red
                var tabletype = "";
                if (item.FlBatal == 1)
                {
                    tabletype = "table-danger";
                }
                // end
                bil++;
        <tr class="@tabletype" style="line-height:0.8rem; page-break-before:avoid;">

            <td class="text-right" style="border-bottom:none;">@bil</td>
            <td class="text-center" style="border-bottom:none;">@Convert.ToDateTime(item.Tarikh).ToString("dd/MM/yyyy")</td>
            <td class="text-center" style="border-bottom:none;">@item.NoPV.Substring(3)</td>
            <td class="text-left text-uppercase" style="border-bottom:none;">@item.Nama</td>

            <td class="text-center text-uppercase">@item1.AkCarta.Kod</td>
            <td class="text-left text-uppercase">@item1.AkCarta.Perihal</td>
            <td class="text-center text-uppercase">@item.JCaraBayar.Perihal</td>
            @if (item.NoCekAtauEFT != null)
            {
                <td class="text-center text-uppercase">@item.NoCekAtauEFT</td>
            }
            else
            {
                <td class="text-center">-</td>
            }
            @if (item.TarCekAtauEFT != null)
            {
                <td class="text-center text-uppercase">@Convert.ToDateTime(item.TarCekAtauEFT).ToString("dd/MM/yyyy")</td>
            }
            else
            {
                <td class="text-center">-</td>
            }


            <td class="text-right ">@Convert.ToDecimal(item1.Amaun).ToString("#,##0.00")</td>
        </tr>
            }

        }
        <tr class="font-weight-bold">
            <td colspan="9" class="table-borderless text-right">JUMLAH RM</td>
            <td class="text-right">@Convert.ToDecimal(Model.JumlahDebit).ToString("#,##0.00")</td>
        </tr>
    </tbody>
</table>

@*ringkasan*@
@*<div>
    <p class="breakhere"></p>
</div>

<div class="row flexrow mb-3 ml-0 text-white">
    <div class="col-2 image-container" hidden>
        <img src="~/img/MainLogo_Syarikat.png" alt="Logo syarikat" />
    </div>
    <div class="col-8 text-uppercase">
        <h5 class="mb-1"><strong>@Model.CompanyDetail.NamaSyarikat</strong></h5>
        <h6 class="mb-0 text-white"> -</h6>
        <h6 class="mb-0 text-white"> -</h6>
        <h6 class="mb-0">Laporan Daftar Resit Terperinci Kump Wang : @Model.JKW.Kod - @Model.JKW.Perihal </h6>
        <h6 class="mb-0">Bagi Tarikh : @Convert.ToDateTime(Model.TarikhDari).ToString("dd/MM/yyyy") -> @Convert.ToDateTime(Model.TarikhHingga).ToString("dd/MM/yyyy")</h6>
    </div>

    <div class="col-2 text-right">
        <h6 class="mb-0">Tarikh: @Convert.ToDateTime(DateTime.Today).ToString("dd/MM/yyyy")</h6>
        <h6 class="mb-0">Laporan: @Model.KodLaporan</h6>
    </div>
</div>


<div class="container">
    <div class="row flexrow">
        <div class="col-4">
            <h6><strong>Ringkasan:</strong></h6>
            <div class="table-responsive-sm mb-0">
                <table class="table table-bordered table-sm topic">

                    <tbody>
                        <tr class="bg-secondary">
                            <td class="text-center font-weight-bold text-white">Jenis Cara Bayar</td>
                            <td class="text-center font-weight-bold text-white">Bilangan</td>
                        </tr>
                        @foreach (var item in Model.LPR00102_1)
                        {

                            <tr>
                                <td class="text-left text-uppercase">@item.Perihal</td>
                                <td class="text-right ">@item.Kuantiti</td>
                            </tr>

                        }
                    </tbody>
                </table>
            </div>
        </div>
        <div class="col-4">
            
        </div>
        <div class="col-4">
        </div>

    </div>
</div>*@

@*ringkasan end*@


<div class="row flexrow mb-3 ml-0">
    <div class="col-md-4">
        <h6 class="mb-5">
            Disediakan Oleh :
        </h6>
        <hr class="mt-2 mr-2" />
        <h6>@Model.Username</h6>
    </div>
    <div class="col-md-4">
        <h6 class="mb-5">
            Disemak Oleh :
        </h6>
        <hr class="mt-2 mr-2" />
    </div>
    <div class="col-md-4">
        <h6 class="mb-5">
            Disahkan Oleh :
        </h6>
        <hr class="mt-2 mr-2" />
    </div>
</div>

