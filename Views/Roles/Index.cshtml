@model IEnumerable<IdentityRole>
<br />
<div class="row mb-2">
    <div class="col-6">
        <h4 class="text-primary">Maklumat Tahap Pengguna</h4>
    </div>
    @*<div class="col-6 text-right">
        <a asp-action="Upsert" asp-controller="Roles" class="btn btn-success">Tambah Baru</a>
    </div>*@
</div>
<div class="p-4 border" rounded>
    @if (Model.Any())
    {
    <div class="table-responsive-md">
        <table class="table table-striped border table-sm">
            <tr class="table-secondary">
                <th width="30%">Id</th>
                <th>Tahap Pengguna</th>
                @*<th></th>*@
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Id
                    </td>
                    <td>
                        @item.Name
                    </td>
                    @*<td>
                        <div class="text-center">
                            <form asp-action="Delete" asp-route-id="@item.Id">
                                <a asp-action="Upsert" asp-route-id="@item.Id" class="btn btn-info text-white btn-sm" style="cursor:pointer">
                                    Ubah
                                </a>

                                <button type="submit" onclick="return confirm('Anda pasti?')" class="btn btn-danger btn-sm">Hapus</button>
                            </form>
                        </div>

                    </td>*@
                </tr>
            }
        </table>

    </div>
    }
</div>